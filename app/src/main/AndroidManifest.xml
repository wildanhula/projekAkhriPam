<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Untuk akses galeri pada SDK < 33 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Untuk akses gambar (SDK 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Untuk akses kamera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projectakhir"
        tools:targetApi="35">

        <!-- Activity login sebagai launcher -->
        <activity
            android:name=".login_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Daftar aktivitas lainnya -->
        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".ReviewActivity" android:exported="true" />
        <activity android:name=".RestoranActivity" android:exported="false" />
        <activity android:name=".FavoriteActivity" android:exported="false" />
        <activity android:name=".ProfilActivity" android:exported="false" />
        <activity android:name=".AddReviewActivity" android:exported="false" />
        <activity android:name=".EditReviewActivity" android:exported="false" />
        <activity android:name=".edit_profile" android:exported="false" />
        <activity android:name=".InsertFoodActivity" android:exported="false" />
        <activity android:name=".EditFoodActivity" android:exported="false" />
        <activity android:name=".InsertRestaurantActivity" android:exported="false" />
        <activity android:name=".EditRestaurantActivity" android:exported="false" />
        <activity android:name=".JournalActivity" android:exported="false" />
        <activity android:name=".InsertJournalActivity" android:exported="false" />



        <!-- Preloaded fonts -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>
</manifest>
